package model;

import java.util.List;

public class Fighter extends Droid {

	public Fighter(String name, int health, int energy, int power, int price, boolean flag) {
		super(name, health, energy, power, price, flag);
	}

	@Override
	public void performance(List<Droid> droids) {
		droids.get(0).getShot(getPower());
		System.out.println(droids.get(0).getName() + " health: " + droids.get(0).getHealth());
		if(droids.get(0).getHealth() < 0){
			droids.remove(0);
		}
	}

	@Override
	public void getShot(int power) {
		setHealth(getHealth() - power); 
	}

}
